// msg_blessing_helper 域 — 1 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.msg_blessing_helper;

// ==== source: msg/blessing_helper/blessing_helper$msg.java ====
message msg {
  uint32 version = 1;
  uint32 result = 2;
  uint64 uin = 3;
  uint64 time = 4;
  uint32 number = 5;
  bytes text = 6;
  uint32 touin_count = 7;
  repeated uint64 touin_list = 8;
}

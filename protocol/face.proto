// face 域 — 2 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.face;

// ==== source: face/FaceForeignIp$AvailableRequest.java ====
message AvailableRequest {
  repeated bytes dims = 1;  // was: enum
  uint32 appID = 2;
  string name = 3;
  string from = 4;
  uint64 uin = 10;
  repeated bytes ip = 11;
  uint32 platform = 12;
  bytes version = 13;
}

// ==== source: face/FaceForeignIp$AvailableResponse.java ====
message AvailableResponse {
  int32 ret = 1;
  repeated bytes msg = 2;
}

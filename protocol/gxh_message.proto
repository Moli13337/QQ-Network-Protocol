// gxh_message 域 — 1 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.gxh_message;

// ==== source: gxh_message/Dialogue.java ====
message Dialogue {
  int32 nameplate_pendant_itemid = 1;
  int32 diyfontid = 2;
  int32 game_nameplate = 3;
  int32 vipstar_flag = 4;
  repeated int32 diy_nameplate_ids = 5;
  int32 qqvadata_changerand = 6;
  int32 aio_badge_show_switch = 7;
}

// nearby_nearbylivebarsvr 域 — 3 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.nearby_nearbylivebarsvr;

// ==== source: nearby/nearbyLivebarSvr/nearbyLivebarSvr/AnchorInfo.java ====
message AnchorInfo {
  string nick = 1;
  string head_url = 2;
  string desc = 3;
  string distance = 4;
  uint32 age = 5;
  string gender = 6;
  string sign = 7;
  string jump_url = 8;
  string jump_desc = 9;
}

// ==== source: nearby/nearbyLivebarSvr/nearbyLivebarSvr/GetLiveBarStatusRequest.java ====
message GetLiveBarStatusRequest {
  uint64 anchor_id = 1;
  string longitude = 2;
  string latitude = 3;
  uint32 need_anchorInfo = 4;
}

// ==== source: nearby/nearbyLivebarSvr/nearbyLivebarSvr/GetLiveBarStatusResponse.java ====
message GetLiveBarStatusResponse {
  int32 ret_code = 1;
  int32 display_livebar = 2;
  AnchorInfo anchor_info = 3;
  int32 need_livebar = 4;
  uint32 time_threshold = 5;
}

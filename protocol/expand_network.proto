// expand_network 域 — 8 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.expand_network;

// ==== source: expand/network/ExtendFriendABTestConfig$ExtendFriendParamsInfo.java ====
message ExtendFriendParamsInfo {
  string key = 1;
  string value = 2;
}

// ==== source: expand/network/ExtendFriendABTestConfig$GetTabTestInfoReq.java ====
message GetTabTestInfoReq {
  string version = 1;
}

// ==== source: expand/network/ExtendFriendABTestConfig$GetTabTestInfoRsp.java ====
message GetTabTestInfoRsp {
  repeated GrayPolicyInfo grayPolicyInfoList = 1;
}

// ==== source: expand/network/ExtendFriendABTestConfig$GrayPolicyInfo.java ====
message GrayPolicyInfo {
  string grayPolicyId = 1;
  repeated ExtendFriendParamsInfo paramsInfoList = 2;
  string sGroupKey = 3;
  string sStrategyType = 4;
  string expKey = 5;
}

// ==== source: expand/network/gateway$SSODownStream.java ====
message SSODownStream {
  repeated bytes head = 1;
  bytes body = 2;
}

// ==== source: expand/network/gateway$SSODownStreamHead.java ====
message SSODownStreamHead {
  int32 code = 1;
  repeated bytes msg = 2;
}

// ==== source: expand/network/gateway$SSOUpStream.java ====
message SSOUpStream {
  repeated bytes head = 1;
  bytes body = 2;
}

// ==== source: expand/network/gateway$SSOUpStreamHead.java ====
message SSOUpStreamHead {
  // (no fields resolved)
}

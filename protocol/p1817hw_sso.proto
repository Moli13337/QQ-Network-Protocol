// p1817hw_sso 域 — 2 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.p1817hw_sso;

// ==== source: p1817hw/sso/hw_sso$HwSSOReq.java ====
message HwSSOReq {
  string cmd = 1;
  bytes body = 2;
}

// ==== source: p1817hw/sso/hw_sso$HwSSORsp.java ====
message HwSSORsp {
  string err_msg = 1;
  bytes body = 2;
}

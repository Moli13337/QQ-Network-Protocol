// msg_uin_list 域 — 5 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.msg_uin_list;

// ==== source: msg/uin_list/UinList$C2C.java ====
message C2C {
  uint64 to_uin = 1;
}

// ==== source: msg/uin_list/UinList$UinListFetchReq.java ====
message UinListFetchReq {
  bytes bytes_store_key = 1;
}

// ==== source: msg/uin_list/UinList$UinListFetchRsp.java ====
message UinListFetchRsp {
  int32 int32_result = 1;
  repeated C2C friend_list = 2;
}

// ==== source: msg/uin_list/UinList$UinListUploadReq.java ====
message UinListUploadReq {
  uint32 uint32_cmd = 1;
  repeated C2C friend_list = 2;
}

// ==== source: msg/uin_list/UinList$UinListUploadRsp.java ====
message UinListUploadRsp {
  int32 int32_result = 1;
  bytes bytes_store_key = 2;
  uint32 silence_period = 3;
}

// msf_onlinepush 域 — 1 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.msf_onlinepush;

// ==== source: msf/onlinepush/msg_onlinepush$PbPushMsg.java ====
message PbPushMsg {
  repeated bytes msg = 1;
  int32 svrip = 2;
  bytes bytes_push_token = 3;
  uint32 ping_flag = 4;
  uint32 uint32_general_flag = 9;
  uint64 uint64_bind_uin = 10;
}

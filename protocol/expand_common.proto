// expand_common 域 — 4 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.expand_common;

// ==== source: expand/common/AddMatchedRaletionReq.java ====
message AddMatchedRaletionReq {
  MatchedInfo UserInfo = 1;
  MatchedInfo MatchUserInfo = 2;
}

// ==== source: expand/common/AddMatchedRaletionRsp.java ====
message AddMatchedRaletionRsp {
  // (no fields resolved)
}

// ==== source: expand/common/GetMatchSrcRsp.java ====
message GetMatchSrcRsp {
  repeated MatchedInfo MatchedInfoList = 1;
  int32 Code = 2;
  string ErrMsg = 3;
}

// ==== source: expand/common/MatchedInfo.java ====
message MatchedInfo {
  uint64 MatchedUin = 1;
  uint64 Timestamp = 2;
  uint32 OldMatchSrcId = 3;
  uint32 MatchSrcId = 4;
  string MatchSrcDes = 5;
  string NewMatchTip = 6;
}

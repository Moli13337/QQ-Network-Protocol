// tencent_hiboom 域 — 4 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.tencent_hiboom;

// ==== source: tencent/hiboom/hiboomauth/hiboom_auth$ImAgentSendNextPack.java ====
message ImAgentSendNextPack_AuthReqBody {
  uint64 u64_uin = 1;
  int32 i32_implat = 2;
  bytes str_version = 3;
  repeated bytes req_elem = 4;
  uint32 u32_product_id = 5;
}

message ImAgentSendNextPack_AuthRspBody {
  int32 i32_result = 1;
  bytes bytes_err_msg = 2;
  bytes bytes_trans_info = 3;
  uint32 u32_business_type = 4;
}

message ImAgentSendNextPack {
  repeated bytes head = 1;
  repeated bytes reqBody = 13;
  repeated bytes rspBody = 14;
}

// ==== source: tencent/hiboom/hiboomauth/hiboom_auth$TTipsInfo.java ====
message TTipsInfo {
  int32 i32_type = 1;
  string str_msg = 2;
  string str_title = 3;
  string str_button = 4;
  string str_url = 5;
  string str_vip_type = 6;
  string str_month = 7;
  string str_aid = 8;
}

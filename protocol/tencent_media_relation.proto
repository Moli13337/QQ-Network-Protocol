// tencent_media_relation 域 — 1 消息（自动还原自 QQ 9.3.55 jadx 反编译源码）
// tag = (field_number << 3) | wire_type；0=varint 1=fixed64 2=len 5=fixed32
// 注：跨域/歧义的嵌套消息引用降级为 bytes，原始类型名保留在行尾注释中
syntax = "proto3";
package qq.tencent_media_relation;

// ==== source: tencent/media_relation/media_relation$MediaRelationInfo.java ====
message MediaRelationInfo {
  uint64 frd_uin = 1;
  uint32 create_time = 2;
}
